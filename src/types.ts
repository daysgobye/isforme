export type PageData = {
    path: string
    messages: string[]
}
export type Addon = {
    code: string
    name: string
    version: string
    description: string
    author: string
    files: string[]
}
